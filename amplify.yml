version: 1
frontend:
  phases:
    build:
      commands:
        - env | grep -e VITE_ > .env
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
